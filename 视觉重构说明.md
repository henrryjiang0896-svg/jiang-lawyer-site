# 视觉与信息架构重构说明

> 项目：`jiang-lawyer-site`
> 重构日期：2026-08-27
> 设计取向：高级、安静、讲证据；克制、留白、信息清晰、排版有序。

---

## 一、设计原则

1. **底色与主色**
   - 暖白（`#fbfaf7`）为主背景；深藏蓝（`#15293f`）为正文、页头、关键区块主色。
   - 强调色仅墨绿（`#2f6b5e`）与低饱和铜（`#9a6a44`），且仅在直接答案框、栏目标签、统计数字下划线、CTA 等极少位置出现。

2. **网格与留白**
   - 容器最大宽 1120px；文章阅读宽 720px（舒适中文阅读范围）。
   - 区块之间以 **1px 细分割线**（`#e7e3d9`）分隔，替代厚重卡片背景。
   - 区块内上下留白 64px（桌面）/44px（移动）。

3. **排版**
   - 系统中文栈：PingFang SC / Microsoft YaHei / Source Han Sans SC；正文 16px / 行高 1.9。
   - 标题稳重：重量 700、字距 -0.01em；h1 最大 42px，h2 最大 27px。
   - 无衬线（中文环境字体差异大，sans 跨平台最稳）。

4. **克制元素**
   - 圆角统一 4px；卡片 1px 边框，**无重阴影**。
   - **不使用**法槌、天平、法院、地球、握手、假头像、AI 人像、传统纹样或装饰图。
   - 无 emoji、无品牌色徽章、无渐变 Banner。

5. **极简动画**
   - 仅保留：body 0.4s 淡入（页面进入过渡）、链接/按钮/卡片 0.15s 颜色与位移 hover。
   - 已移除原有的"阅读进度条"与全屏动画脚本（不符合"极少动画"原则）。

6. **可达性**
   - 跳转链接、可见焦点环（2px 墨绿）、aria 标签、`prefers-reduced-motion` 禁用过渡。

---

## 二、核心页面改动

### 1. 首页（`src/pages/index.astro`）
- 首屏改为编辑型两栏布局（无 Banner、无渐变）：
  - 左：eyebrow「跨境税务与出海合规律师」+ 大标题 + 描述 + 身份行（`上海曼昆（深圳）律师事务所｜7 年金融从业｜5 年法律实务`）+ `阅读专业文章 / 联系咨询` 两按钮。
  - 右：极简信息模块（白底 1px 边框，列出 3 个栏目入口，带 →）。
- 「我关注的问题」：5 个问题型文章标题的简洁编号列表（01–05），hover 行高亮。
- 「专业背景」：4 个数字 + 短句（`7年 / 5年 / 2年 / 5年`）以 1px 网格分隔的数字块呈现。
- 「服务与能力」：2 列文字模块，无图标堆砌。
- 「最新文章」：研究报告目录式列表——栏目 · 标题 · 摘要 · 更新时间 · 箭头链接。
- 「联系咨询」：深藏蓝区块，白字，强调 `提交咨询不构成委托关系，请勿发送保密材料或敏感信息`。

### 2. 文章页（`src/pages/articles/[slug].astro`）
- 出版物式排版：
  - 标题上方显示栏目（小号墨绿 kicker）+ 更新于时间。
  - 标题大而清晰；meta 显示 `江恒律师｜上海曼昆（深圳）律师事务所律师 · 更新于 …`。
  - 「直接答案」为墨绿左边框的浅色提示框。
  - 信息截至日期提示框。
- 长文目录：
  - 桌面端：左侧 sticky 目录（细左边线）。
  - 移动端：`<details>` 折叠目录，位于标题正下方，无需 JS。
- 表格：横向滚动（`display:block; overflow-x:auto`），细边框、轻表头底色。
- 统一低干扰样式：资料来源（在正文内）、作者卡（窄边框 + 方形头像块）、免责声明（纸底）、相关文章（目录列表）。
- 文章底部 **单一联系咨询 CTA**（深藏蓝区块）。

### 3. 关于页（`src/pages/about.astro`）
- 职业档案式：`page-head`（eyebrow / 标题 / lead）+ 复合经历（双卡）+ 专业关注领域（列表）+ 已发布文章（目录列表）+ 执业机构与联系（含公开邮箱，附带"执业地区/执业证号/头像确认前不展示"说明）。

### 4. 联系咨询（`src/pages/contact.astro`）
- 深藏蓝 `navy-block` 展示已公开邮箱与安全提示（`mailto:` 可点击）。
- 纯前端示意表单：拦截提交并提示"本站点不提供在线提交"。
- 保持不收集任何数据（无后端）。

### 5. 服务与能力 / 文章中心 / 栏目页
- 统一 `page-head` + `dir-list` 模式。
- 栏目页用纸底克制清单替代原先的深蓝 `bg-banner`。

### 6. 导航 / 页脚
- 顶部主导航简化为 **4 项**：`文章 / 服务与能力 / 关于江恒 / 联系咨询`。
- 底部页脚改为纸底细线分隔，含：品牌简介 + 公开邮箱 + 栏目与页面链接 + 免责说明 + 版权。

### 7. 文章正文模板段处理（如实记录）
- 原 5 篇 Markdown 在正文末尾各含三段模板：`## 作者信息` / `## 免责声明` / `## 咨询 CTA`。
- 这三段已统一由文章页组件接管（`AuthorCard` / `Disclaimer` / 单一 CTA 区块），为避免与组件重复渲染，本次将这三段从 **正文** 与 **导入脚本（`scripts/import-articles.mjs`）** 中同步剥离。文章实质内容（背景、风险点、正文、FAQ、资料来源）**完整保留**。

---

## 三、已写入的真实信息

| 字段 | 值 | 写入位置 |
|------|------|------|
| 真实姓名 | `江恒` | `siteConfig.author.realName`（驱动 Person / Article author / publisher / provider） |
| 品牌名 | `江恒律师` | `siteConfig.display.penName`，全站品牌位、Title、OG site_name |
| 执业机构 | `上海曼昆（深圳）律师事务所` | `siteConfig.author.lawFirm`，首页身份行、关于页、JSON-LD affiliation |
| 公开邮箱 | `j.heng@hotmail.com` | 联系页 navy-block、页脚"公开邮箱"、Person JSON-LD（`email` 字段） |

> `siteConfig.author.avatar / phone / licenseNo / barAdmission / social.*` 仍为空，未填入任何虚构信息；上线前由江恒律师确认后填入。

### 结构化数据（条件化输出）
- `Person`：name=`江恒`，affiliation=`Organization 曼昆律所`，jobTitle=`上海曼昆（深圳）律师事务所律师`，email 已写入；URL 字段因 `domain` 未填而不输出。
- `Article`：author / publisher = `Person(name=江恒, jobTitle=…)`；mainEntityOfPage 因 `domain` 未填而不输出。
- `LegalService`：provider = `Person(name=江恒)`；URL 字段同上。
- 律所一律使用 `affiliation`，**未使用 `alumniOf`**。

---

## 四、域名与上线

- `siteConfig.domain` 仍为空。
- 全站 **未输出** `canonical`、`<og:url>`、sitemap、RSS、JSON-LD URL 字段。
- 构建时 sitemap / RSS 安全跳过并打印提示：`domain 未配置，已安全跳过 …，未写入 占位链接`。
- 真实上线前由江恒律师在 `src/site.config.ts` 填写 `domain`（HTTPS 正式域名）后重新 `npm run build` 即可，无需改代码。

### 上线前必填字段（`src/site.config.ts`）
1. `domain`（HTTPS 正式域名）—— 启用 canonical / og:url / sitemap / RSS / JSON-LD URL。
2. 已确认：`author.realName = "江恒"`、`author.lawFirm = "上海曼昆（深圳）律师事务所"`、`author.email = "j.heng@hotmail.com"`（已写入）。
3. 选填（仅在确认后填）：`barAdmission / licenseNo / phone / avatar / social.*`。

---

## 五、构建与验证结果

- `npm run import` 5/5 篇成功；`npm run build` 14 页成功。
- 校验（dist 全文）：
  - `example.com`：**0**
  - `alumniOf`：**0**
  - 旧名 `江律师`（HTML）：**0**（注释与构建产物中已全部替换）
  - 旧律所 `曼昆律师事务所`：**0**
  - 品牌 `江恒律师` 出现：**161** 处
  - 律所 `上海曼昆（深圳）律师事务所`：**44** 处
  - 邮箱 `j.heng@hotmail.com`：**34** 处（联系页 + 页脚 + Person JSON-LD）
  - `Person @type` 出现：**13** 处（realName 已确认后启用）
  - `canonical` / `og:url`：**0**（domain 未配置）

---

## 六、桌面与移动端截图

存放于 `jiang-lawyer-site/screenshots/`（由 Playwright + Chromium Headless Shell 生成，整页截图）：

- `home-desktop.png` / `home-mobile.png`
- `article-desktop.png` / `article-mobile.png`
- `about-desktop.png` / `about-mobile.png`

移动端核验：
- 导航收起为 `菜单` 折叠。
- 首屏信息模块下沉至主标题下方。
- 文章页目录以 `<details>` 折叠于标题下；表格横向滚动。
- 联系 / 预约 CTA 在窄屏正常。

---

## 七、如实记录：蓝图文件误伤与恢复

- 在用脚本批量处理 10 篇文章 Markdown 时，误把工作区根目录的规划文档 `江律师个人网站蓝图.md` 当成文章处理：其第 8 节"内容生产模板"的代码块内含有 `## 作者信息 / 免责声明 / 咨询 CTA` 三个模板子节，被脚本当作文章模板段截断。
- 该文件 **不在** `jiang-lawyer-site/` 项目范围内；误伤部分仅为蓝图第 8 节模板末尾。
- 已立即按蓝图原有模板格式恢复补全末尾三个子节与闭合围栏，文档已恢复连贯。后续在自动化处理 Markdown 时应严格限定输入为 `src/content/articles/` 与本次明确的 5 个文件名，避免再触及规划/审校等非文章文件。